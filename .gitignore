# Debian build artifacts
debian/build/
debian/*.debhelper
debian/*.substvars
debian/files
debian/debhelper-build-stamp
*.deb
*.dsc
*.changes
*.buildinfo
*.tar.xz
*.tar.gz
*.tar.zst
../*.deb
../*.dsc
../*.changes
../*.buildinfo

# Rust
target/
Cargo.lock.bak
**/*.rs.bk

# pbuilder / sbuild
*.build
*.upload
/build/

# Editor / OS junk
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Secrets and local overrides
.env
.env.local
*.key
*.pem
credentials.json

# Upstream source (fetched by scripts, not committed)
upstream/

# Fork working trees (built from base tag + patches/*/series)
fork/

# Test-install state records
/var/lib/asus-phase1-fork/

# Claude Code session state
.claude/
